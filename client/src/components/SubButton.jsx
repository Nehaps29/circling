import React from 'react';
import Button from '@mui/material/Button';
import './SubButton.css'; // Import the CSS file

const SubButton = () => (
  <Button variant="contained" color="primary" className="sub-button">
    Subscribe
  </Button>
);

export default SubButton;
